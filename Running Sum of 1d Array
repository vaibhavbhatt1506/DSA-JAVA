class Solution {
    public int[] runningSum(int[] nums) {
        for (int i = 1; i < nums.length; i++) {
            // Current element becomes the sum of itself and the previous element
            nums[i] += nums[i - 1];
        }
        return nums;
    }
}
